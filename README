Navigate
========

Allows you to identify navigation namespaces in your controllers, and
render navigation items in your views.


Basic Example
=============

In your controller:

	class PagesController < ApplicationController
		navigate :main_menu, :pages
	end

	class PostsController < ApplicationController
  	navigate :main_menu, :posts
	end
	
In your view:

  <% navigate :main_menu, :pages do %>
    <%= link_to 'Pages', pages_path %>
  <% end %>

  <% navigate :main_menu, :posts do %>
  	<%= link_to 'Users', users_path %>
  <% end %>

Assuming the request routed to the PagesController, the view will
render as:

	<li class="nav_item selected">
		<a href="/pages">Pages</a>
	</li>
	
	<li class="nav_item">
		<a href="/posts">Posts</a>
	</li>
	

Navigate Independently
======================

Controller actions can navigate independently of each other:

	class PostsController
		navigate :sub_menu, :show, :only => :show
		navigate :sub_menu, :edit, :only => :edit
	end
	

Conditional Rendering
=====================

In your view, you can choose when to render a navigation item using the
if and unless options.

Example using unless:

  <% navigate :main_menu, :users, :unless => !authorized? do %>
	  ...
	<% end %>
	
Example using if:
	
	<% navigate :main_menu, :users, :if => authorized? do %>
	  ...
	<% end %>
	
	
Customize Navigation Items
==========================

You can specify a different HTML tag for your navigation items using
the tag option. For example:

	<% navigate :main_menu, :pages, :tag => :div do %>
		...
	<% end %>
	
Will render as:

	<div class="nav_item">
		...
	</div>


Copyright (c) 2009 Jonathan Sutherland, released under the MIT license
